# Setup plot 
set terminal png

set xlabel "x"
set output 'plot.png'


# Make plot
plot 'result.dat' index 0 title "yi(xi), data points", \
'result.dat' index 1 using 1:2 with line title "y(x), quadratic spline",\
'result.dat' index 1 using 1:3 with line title "dy/dx(x)",\
'result.dat' index 1 using 1:4 with line title "int(y(x)) from x0 to x"


