# Setup plot 
set terminal png

set xlabel "x"
set ylabel "gamma(x)"
set title "Gamma function"

set xrange [-5:5]
set yrange [-10:10]

set output 'plot.png'


# Make plot
plot 'gamma_val.dat' with line title "gamma(x)", 'tabval.dat' with points pointtype 3 title "table values"
